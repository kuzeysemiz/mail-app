version: '3.8'

services:
  # Backend API
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mail_api
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=5000
      - GMAIL_EMAIL=${GMAIL_EMAIL}
      - GMAIL_PASSWORD=${GMAIL_PASSWORD}
    ports:
      - "5000:5000"
    volumes:
      - ./backend/mail_scheduler.db:/app/mail_scheduler.db
      - ./backend/logs:/app/logs
    networks:
      - mail_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend (React)
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_API_URL=http://api:5000/api
    container_name: mail_web
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - api
    networks:
      - mail_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  mail_network:
    driver: bridge

volumes:
  db_data:
    driver: local
  logs_data:
    driver: local
